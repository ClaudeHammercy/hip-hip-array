const mixedSignNumbers = [3, 15, 14, -2, -3, -8, -103, 4];

for (let i = 0; i <= mixedSignNumbers.length - 1; 
    i++) {
    if (mixedSignNumbers[i] % 2 === 0) {
    console.log(mixedSignNumbers[i]);
}
}